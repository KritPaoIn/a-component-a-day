const HelloWorld = () => {
  return (
    <div>
      <button className="hover:text-accent cursor-pointer select-none text-2xl font-bold transition-colors">
        Hello, World!
      </button>
    </div>
  );
};

export default HelloWorld;
